#include <stdio.h>
#include <string.h>
#include<stdlib.h>


int main()
{
    /*
    char first_str[] = "01234I5678H90123";
    const char second_str[] = "IH";
 
    // printing the strings
    printf("First String: %s\n", first_str);
    printf("Second String: %s\n", second_str);
 
    // printing the return value of the strcmp()
    strstr(first_str, second_str);

    printf("First String: %s\n", first_str);
    printf("Second String: %s\n", second_str);


    // printf("Return value of strstr(): %s\n", strtok(first_str, second_str));
    char* token = strtok(first_str, second_str); 
    while (token != NULL) { 
        printf("Token: %s\n", token); 
        token = strtok(NULL, second_str); 
    } 

    int i = 255;
    char *ptr = &i;
    printf("%d\n", *ptr); 
    printf("%d", (char)255);
    return 0;
    */

   double x = 5.6;
   x++;
   printf("%lf\n", x);

}